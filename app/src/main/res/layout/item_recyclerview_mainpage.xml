<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="@dimen/fat_margin"
    android:layout_marginVertical="4dp">

    <Button
        android:id="@+id/fragmentButton"
        android:backgroundTint="#a6a4a4"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintHorizontal_weight="50"
        android:text="TEXT"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/deleteButton"/>

    <Button
        android:id="@+id/deleteButton"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:backgroundTint="#ad2a2a"
        app:layout_constraintHorizontal_weight="10"
        android:text="X"
        app:layout_constraintTop_toTopOf="@id/fragmentButton"
        app:layout_constraintBottom_toBottomOf="@id/fragmentButton"
        app:layout_constraintStart_toEndOf="@id/fragmentButton"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>